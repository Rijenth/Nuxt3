version: '3.8'

services:
  nuxt:
    container_name: nuxt
    image: nuxt_dev
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - ./frontend:/frontend
      - /frontend/node_modules
    ports:
      - 3000:3000
    command: yarn dev
    restart: always